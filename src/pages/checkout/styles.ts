import { styled } from 'styled-components'

export const StyledPage = styled.main`
	max-width: 112rem;
	width: 100%;
	margin-inline: auto;
	margin-top: 3.2rem;
	gap: 3.2rem;
	display: flex;
	flex-wrap: wrap;
	align-items: start;
`
